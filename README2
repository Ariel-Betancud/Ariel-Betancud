###Comandos para Kali Linux

```sh
nmap -> Nos permite escanear y recolectar información
ifconfig -> sabemos nuestra IP
ping 192.168.x.x   -> la IP a ver
nmap -sP -> hace un barrido de toda una red, realizando ping
nmap -sP 192.168.1.0/24 -> si hace ping
nmap -sn 192.168.1.0/24 -> es sin hacer ping
barra espaciadora para mostrar la información
nmap -Pn 192.168.1.x -> nuestra IP o una especifica analiza los 1024 puertos
```
